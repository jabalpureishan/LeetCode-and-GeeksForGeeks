class Node:
    def __init__(self,data):
        self.data = data
        self.next = None
class Solution:
    def insertatbegin(head,x):
        temp = Node(temp.x)
        temp.next = head
        head = temp.data
    def insertatenf(head,x):
        ans = head.data
        while(ans.next!=N):
            ans = ans.next
        temp = Node(temp.x)
        ans.next = temp.data
